<div class="form">  
  <h1>Editing Album</h1>

  <%= form_for(@album) do |f| %>

  <% if @album.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@album.errors.count, "error") %> prohibited this album from being saved:
    </h2>
    <ul>
      <% @album.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="form-group">
    <%= f.label :Artist %><br>
    <%= collection_select( :artist, :artist_id, Artist.all, :id, :artistName, :class => 'form-control' )%>  </div>

  <div class="form-group">
    <%= f.label :Album_Name %><br>
    <%= f.text_field :albumName, :class => 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :Year_of_Release %><br>
    <%= f.text_field :albumYear, :class => 'form-control' %>
  </div>

  <!--<p>
    <%= f.label :Ano_de_Criação %><br>
    <%= select_year Date.today, :start_year => Time.now.year, :end_year => Time.now.year - 95, :field_name => :grad_year, :prefix => :profile %>

  </p>-->
  


  <p><%= f.submit :class => "btn btn-success btn-mini" %>
    <%= link_to 'Back', albums_path, :class => "btn btn-warning btn-mini" %></p>

    <% end %>

  </div>